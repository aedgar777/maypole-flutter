<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme for Android 12+ using the new Splash Screen API (Dark Mode) -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color -->
        <item name="windowSplashScreenBackground">@color/dark_purple</item>
        <!-- Set the splash screen icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo_splash</item>
        <!-- Optional: Set icon background color (if you want a circle behind the icon) -->
        <!-- <item name="windowSplashScreenIconBackgroundColor">@color/dark_purple</item> -->
        <!-- Post splash screen theme -->
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
        <!-- Enable edge-to-edge with transparent system bars -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <!-- Enable edge-to-edge with transparent system bars -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
