<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Notification permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Internet permission for map tiles -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Enable Flutter's built-in deep linking so App Links/Universal Links
                 are handed to go_router and open the matching screen in-app.
                 This is the Android counterpart to FlutterDeepLinkingEnabled (iOS). -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="true"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- App Links for Production (maypole.app) -->
            <!-- Auth-action links (/auth-action.html) are handled by the browser,
                 never the Flutter app. We enumerate the app routes instead of a
                 catch-all so the OS doesn't open the app for password reset /
                 email verification URLs. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Single-segment app routes -->
                <data android:scheme="https" android:host="maypole.app" android:path="/" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/login" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/register" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/email-verified" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/home" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/search" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/settings" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/privacy-policy" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/child-safety-standards" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/help" />

                <!-- Multi-segment app routes -->
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/chat/" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/dm/" />
                <data android:scheme="https" android:host="maypole.app" android:pathPrefix="/user-profile/" />

                <!-- Semantic share links: /:locationSlug/:placeSlug -->
                <data android:scheme="https" android:host="maypole.app" android:pathPattern="/.*/.*" />
            </intent-filter>

            <!-- App Links for Dev (maypole-flutter-dev.web.app) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Single-segment app routes -->
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:path="/" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/login" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/register" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/email-verified" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/home" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/search" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/settings" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/privacy-policy" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/child-safety-standards" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/help" />

                <!-- Multi-segment app routes -->
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/chat/" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/dm/" />
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPrefix="/user-profile/" />

                <!-- Semantic share links: /:locationSlug/:placeSlug -->
                <data android:scheme="https" android:host="maypole-flutter-dev.web.app" android:pathPattern="/.*/.*" />
            </intent-filter>
            
            <!-- Custom URL scheme (fallback) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <data
                    android:scheme="maypole"
                    android:host="chat" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Firebase Cloud Messaging notification icon and color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <!-- Google Mobile Ads App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9803674282352310~3165030367"/>
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA7kcPWjaVK3iC4qbmSO1vuTBEk11llq9A"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to query email clients -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
</manifest>
