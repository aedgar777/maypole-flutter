# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Environment files with sensitive data
.env
.env.dev
.env.prod
.env.local

# Android keystore and signing files (NEVER commit these!)
android/key.properties
android/app/*.jks
android/app/*.keystore
*.jks
*.keystore

# Keystore backup documentation (contains sensitive info)
KEYSTORE_BACKUP.md
.env.production

# Firebase configuration files with sensitive data
/android/app/google-services.json
/ios/Runner/GoogleService-Info.plist
/macos/Runner/GoogleService-Info.plist

# iOS signing files
ios/*.p12
ios/*.mobileprovision
ios/*.cer
ios/*.certSigningRequest
ios/AuthKey_*.p8
ios/AuthKey.json

# Fastlane
ios/fastlane/report.xml
ios/fastlane/Preview.html
ios/fastlane/screenshots
ios/fastlane/test_output
ios/.env.local
ios/vendor/bundle

# Local properties (contains SDK paths)
android/local.properties

# Android signing keys
android/key.properties
android/app/*.jks
android/app/*.keystore
*.jks
*keystore*.jks
keystore_base64.txt
*_base64.txt

# Sensitive Firebase files
firebase-debug.log
firebase-debug.*.log

# Functions environment
/functions/.env*
!/functions/.env.example

# Python virtual environment (functions)
/functions/venv/
/functions/__pycache__/

# Any backup files of sensitive data
*.backup
firebase_options.dart.backup

# Team secrets management
.secrets-project

# Firebase project configuration
# Note: .firebaserc should be gitignored if it contains sensitive project IDs
# For open source or when project IDs are not sensitive, you can commit it
# See .firebaserc for template
.firebaserc
