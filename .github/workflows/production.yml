name: Production Deployment

on:
  push:
    branches:
      - master
      - main

jobs:
  build_web:
    name: Build and Deploy Web (Production)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Validate Version Format
        run: |
          chmod +x scripts/validate-version.sh scripts/get-version.sh
          echo "🔍 Validating version..."
          ./scripts/validate-version.sh
          echo ""
          echo "📱 Current version:"
          ./scripts/get-version.sh

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: 'stable'

      - name: Flutter pub get
        run: flutter pub get

      - name: Create .env file from secrets
        run: |
          cat > .env << EOF
          # Production Environment Configuration (Generated by GitHub Actions)
          ENVIRONMENT=production
          
          # Firebase Production Configuration
          FIREBASE_PROD_WEB_API_KEY=${{ secrets.FIREBASE_PROD_WEB_API_KEY }}
          FIREBASE_PROD_WEB_APP_ID=${{ secrets.FIREBASE_PROD_WEB_APP_ID }}
          FIREBASE_PROD_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_PROD_WEB_MEASUREMENT_ID }}
          FIREBASE_PROD_ANDROID_API_KEY=${{ secrets.FIREBASE_PROD_ANDROID_API_KEY }}
          FIREBASE_PROD_ANDROID_APP_ID=${{ secrets.FIREBASE_PROD_ANDROID_APP_ID }}
          FIREBASE_PROD_IOS_API_KEY=${{ secrets.FIREBASE_PROD_IOS_API_KEY }}
          FIREBASE_PROD_IOS_APP_ID=${{ secrets.FIREBASE_PROD_IOS_APP_ID }}
          FIREBASE_PROD_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_PROD_MESSAGING_SENDER_ID }}
          FIREBASE_PROD_PROJECT_ID=${{ secrets.FIREBASE_PROD_PROJECT_ID }}
          FIREBASE_PROD_AUTH_DOMAIN=${{ secrets.FIREBASE_PROD_AUTH_DOMAIN }}
          FIREBASE_PROD_STORAGE_BUCKET=${{ secrets.FIREBASE_PROD_STORAGE_BUCKET }}
          IOS_BUNDLE_ID=${{ secrets.IOS_BUNDLE_ID }}
          EOF

      - name: Build Web
        run: |
          flutter build web --release \
            --dart-define=ENVIRONMENT=production \
            --dart-define=FIREBASE_PROD_WEB_API_KEY=${{ secrets.FIREBASE_PROD_WEB_API_KEY }} \
            --dart-define=FIREBASE_PROD_WEB_APP_ID=${{ secrets.FIREBASE_PROD_WEB_APP_ID }} \
            --dart-define=FIREBASE_PROD_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_PROD_MESSAGING_SENDER_ID }} \
            --dart-define=FIREBASE_PROD_PROJECT_ID=${{ secrets.FIREBASE_PROD_PROJECT_ID }} \
            --dart-define=FIREBASE_PROD_AUTH_DOMAIN=${{ secrets.FIREBASE_PROD_AUTH_DOMAIN }} \
            --dart-define=FIREBASE_PROD_STORAGE_BUCKET=${{ secrets.FIREBASE_PROD_STORAGE_BUCKET }} \
            --dart-define=FIREBASE_PROD_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_PROD_WEB_MEASUREMENT_ID }}

      - name: Deploy Firebase Services (Production)
        run: |
          echo '${{ secrets.MAYPOLE_FIREBASE_SERVICE_ACCOUNT }}' > $HOME/gcloud-service-key.json
          export GOOGLE_APPLICATION_CREDENTIALS=$HOME/gcloud-service-key.json
          npm install -g firebase-tools
          
          # Deploy all Firebase services to production
          firebase deploy --only firestore:indexes,firestore:rules,storage:rules --project maypole-flutter-ce6c3 --non-interactive

      - name: Deploy to Firebase Hosting (Production)
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          fireBaseServiceAccount: ${{ secrets.MAYPOLE_FIREBASE_SERVICE_ACCOUNT }}
          projectId: maypole-flutter-ce6c3
          channelId: live

  build_android:
    name: Build and Deploy Android (Production)
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required to push version bump commits
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Auto-Bump Build Number
        run: |
          chmod +x scripts/auto-bump-build.sh scripts/get-version.sh
          
          # Configure git first
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Pull latest changes BEFORE making any modifications
          echo "🔄 Pulling latest changes from master..."
          git pull --rebase origin master || {
            echo "⚠️ Pull failed, but continuing with version bump..."
          }
          
          echo "📱 Current version:"
          ./scripts/get-version.sh
          echo ""
          echo "🔄 Auto-bumping build number..."
          ./scripts/auto-bump-build.sh
          echo ""
          echo "✅ New version:"
          ./scripts/get-version.sh
          
          # Check if there are actually changes to commit
          if git diff --quiet pubspec.yaml; then
            echo "ℹ️ No version changes detected, skipping commit"
            exit 0
          fi
          
          # Commit and push the version bump
          git add pubspec.yaml
          git commit -m "chore: auto-bump build number to $(grep '^version:' pubspec.yaml | sed 's/version: //' | tr -d ' ') [skip ci]"
          
          # Push with retry logic in case of race condition
          for i in {1..3}; do
            if git push origin master; then
              echo "✅ Successfully pushed version bump"
              exit 0
            else
              echo "⚠️ Push failed (attempt $i/3), pulling and retrying..."
              git pull --rebase origin master
            fi
          done
          
          echo "❌ Failed to push after 3 attempts"
          exit 1

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: 'stable'

      - name: Get dependencies
        run: flutter pub get

      - name: Create .env file from secrets
        run: |
          cat > .env << EOF
          ENVIRONMENT=prod
          FIREBASE_PROD_WEB_API_KEY=${{ secrets.FIREBASE_PROD_WEB_API_KEY }}
          FIREBASE_PROD_WEB_APP_ID=${{ secrets.FIREBASE_PROD_WEB_APP_ID }}
          FIREBASE_PROD_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_PROD_WEB_MEASUREMENT_ID }}
          FIREBASE_PROD_ANDROID_API_KEY=${{ secrets.FIREBASE_PROD_ANDROID_API_KEY }}
          FIREBASE_PROD_ANDROID_APP_ID=${{ secrets.FIREBASE_PROD_ANDROID_APP_ID }}
          FIREBASE_PROD_IOS_API_KEY=${{ secrets.FIREBASE_PROD_IOS_API_KEY }}
          FIREBASE_PROD_IOS_APP_ID=${{ secrets.FIREBASE_PROD_IOS_APP_ID }}
          FIREBASE_PROD_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_PROD_MESSAGING_SENDER_ID }}
          FIREBASE_PROD_PROJECT_ID=${{ secrets.FIREBASE_PROD_PROJECT_ID }}
          FIREBASE_PROD_AUTH_DOMAIN=${{ secrets.FIREBASE_PROD_AUTH_DOMAIN }}
          FIREBASE_PROD_STORAGE_BUCKET=${{ secrets.FIREBASE_PROD_STORAGE_BUCKET }}
          IOS_BUNDLE_ID=${{ secrets.IOS_BUNDLE_ID }}
          EOF

      - name: Create google-services.json for prod flavor
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON_PROD }}
        run: |
          mkdir -p android/app/src/prod
          
          # Write the secret to file using environment variable (safer than command substitution)
          echo "$GOOGLE_SERVICES_JSON" > android/app/src/prod/google-services.json
          
          # Debug: Show file info
          echo "Debug: File size: $(wc -c < android/app/src/prod/google-services.json) bytes"
          echo "Debug: First 100 chars:"
          head -c 100 android/app/src/prod/google-services.json
          echo ""
          echo "Debug: Last 100 chars:"
          tail -c 100 android/app/src/prod/google-services.json
          echo ""
          
          # Validate the JSON is well-formed
          echo "Validating JSON..."
          if ! python3 -m json.tool android/app/src/prod/google-services.json > /dev/null 2>&1; then
            echo "❌ Error: google-services.json is not valid JSON"
            echo ""
            echo "Full file contents:"
            cat android/app/src/prod/google-services.json
            echo ""
            echo "JSON parsing error:"
            python3 -m json.tool android/app/src/prod/google-services.json 2>&1 || true
            echo ""
            echo "💡 Tip: Your GOOGLE_SERVICES_JSON_PROD secret might:"
            echo "  - Have extra quotes around the entire JSON"
            echo "  - Be stored as a string instead of raw JSON"
            echo "  - Have escape characters that need to be removed"
            echo ""
            echo "To fix: Copy the ENTIRE contents of your google-services.json file"
            echo "and paste it directly into the GitHub secret (no extra quotes)"
            exit 1
          fi
          
          echo "✅ google-services.json created and validated successfully"

      # Android Signing with Google Play App Signing
      # This workflow uses the same upload keystore for all environments.
      # The secrets should already be configured from the develop workflow setup.
      - name: Setup Android Signing
        run: |
          # Remove any whitespace/newlines and decode
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | tr -d '\n\r ' | base64 -d > android/app/upload-keystore.jks
          
          # Verify the keystore was created successfully
          if [ ! -f android/app/upload-keystore.jks ]; then
            echo "Error: Failed to create keystore file"
            exit 1
          fi
          
          FILESIZE=$(stat -f%z android/app/upload-keystore.jks 2>/dev/null || stat -c%s android/app/upload-keystore.jks 2>/dev/null)
          echo "Keystore file created successfully (size: $FILESIZE bytes)"
          
          # Create key.properties with proper escaping for special characters
          cat > android/key.properties << 'KEYSTORE_EOF'
          storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          storeFile=upload-keystore.jks
          KEYSTORE_EOF

      - name: Build Android App Bundle (prodRelease)
        run: flutter build appbundle --release --flavor prod

      # Upload to Play Store Production Track as DRAFT
      # This uploads the AAB but doesn't automatically release it.
      # You can then manually:
      # - Edit store listing details
      # - Review release notes
      # - Set up staged rollout (recommended: start at 5-10%)
      # - Publish when ready
      - name: Upload to Play Store Production Track (Draft)
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_SERVICE_ACCOUNT_JSON }}
          packageName: app.maypole.maypole
          releaseFiles: build/app/outputs/bundle/prodRelease/app-prod-release.aab
          track: production
          status: draft
          # Optional: You can also add release notes that will be pre-filled
          # whatsNewDirectory: distribution/whatsnew

  # TODO: Uncomment iOS deployment when Apple Developer credentials are available
  # build_ios:
  #   name: Build and Deploy iOS (Production)
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #
  #     - name: Setup Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.32.0'
  #         channel: 'stable'
  #
  #     - name: Get dependencies
  #       run: flutter pub get
  #
  #     - name: Create .env file from secrets
  #       run: |
  #         cat > .env << EOF
  #         ENVIRONMENT=prod
  #         FIREBASE_PROD_WEB_API_KEY=${{ secrets.FIREBASE_PROD_WEB_API_KEY }}
  #         FIREBASE_PROD_WEB_APP_ID=${{ secrets.FIREBASE_PROD_WEB_APP_ID }}
  #         FIREBASE_PROD_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_PROD_WEB_MEASUREMENT_ID }}
  #         FIREBASE_PROD_ANDROID_API_KEY=${{ secrets.FIREBASE_PROD_ANDROID_API_KEY }}
  #         FIREBASE_PROD_ANDROID_APP_ID=${{ secrets.FIREBASE_PROD_ANDROID_APP_ID }}
  #         FIREBASE_PROD_IOS_API_KEY=${{ secrets.FIREBASE_PROD_IOS_API_KEY }}
  #         FIREBASE_PROD_IOS_APP_ID=${{ secrets.FIREBASE_PROD_IOS_APP_ID }}
  #         FIREBASE_PROD_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_PROD_MESSAGING_SENDER_ID }}
  #         FIREBASE_PROD_PROJECT_ID=${{ secrets.FIREBASE_PROD_PROJECT_ID }}
  #         FIREBASE_PROD_AUTH_DOMAIN=${{ secrets.FIREBASE_PROD_AUTH_DOMAIN }}
  #         FIREBASE_PROD_STORAGE_BUCKET=${{ secrets.FIREBASE_PROD_STORAGE_BUCKET }}
  #         IOS_BUNDLE_ID=${{ secrets.IOS_BUNDLE_ID }}
  #         EOF
  #
  #     - name: Create GoogleService-Info.plist for prod
  #       run: |
  #         mkdir -p ios/Runner
  #         echo "${{ secrets.GOOGLE_SERVICE_INFO_PLIST_PROD }}" > ios/Runner/GoogleService-Info.plist
  #         
  #         # Validate the plist is well-formed
  #         if ! plutil -lint ios/Runner/GoogleService-Info.plist > /dev/null 2>&1; then
  #           echo "Error: GoogleService-Info.plist is not valid"
  #           cat ios/Runner/GoogleService-Info.plist
  #           exit 1
  #         fi
  #         
  #         echo "GoogleService-Info.plist created successfully"
  #
  #     # TODO: Configure iOS code signing
  #     # You will need to:
  #     # 1. Create certificates and provisioning profiles in Apple Developer Console
  #     # 2. Export them and add to GitHub Secrets (see DEPLOYMENT_SETUP.md)
  #     # 3. Add IOS_CERTIFICATE_BASE64 to GitHub Secrets
  #     # 4. Add IOS_CERTIFICATE_PASSWORD to GitHub Secrets
  #     # 5. Add IOS_PROVISIONING_PROFILE_BASE64 to GitHub Secrets
  #     # 6. Add KEYCHAIN_PASSWORD to GitHub Secrets
  #     - name: Setup iOS Signing
  #       run: |
  #         # Decode certificate and provisioning profile
  #         echo "${{ secrets.IOS_CERTIFICATE_BASE64 }}" | base64 -d > certificate.p12
  #         echo "${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}" | base64 -d > provisioning.mobileprovision
  #         
  #         # Create temporary keychain
  #         security create-keychain -p "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
  #         security default-keychain -s build.keychain
  #         security unlock-keychain -p "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
  #         security set-keychain-settings -t 3600 -u build.keychain
  #         
  #         # Import certificate
  #         security import certificate.p12 -k build.keychain -P "${{ secrets.IOS_CERTIFICATE_PASSWORD }}" -T /usr/bin/codesign
  #         security set-key-partition-list -S apple-tool:,apple: -s -k "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
  #         
  #         # Install provisioning profile
  #         mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
  #         cp provisioning.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
  #
  #     - name: Build iOS IPA (prodRelease)
  #       run: |
  #         flutter build ipa --release --flavor prod --export-options-plist=ios/ExportOptions.plist
  #
  #     # TODO: Configure App Store Connect API for production release
  #     # You will need to:
  #     # 1. Create an App Store Connect API Key (see DEPLOYMENT_SETUP.md)
  #     # 2. Add APP_STORE_CONNECT_API_KEY_ID to GitHub Secrets
  #     # 3. Add APP_STORE_CONNECT_API_ISSUER_ID to GitHub Secrets
  #     # 4. Add APP_STORE_CONNECT_API_KEY_CONTENT to GitHub Secrets (base64 encoded .p8 file)
  #     - name: Upload to App Store
  #       run: |
  #         # Decode API key
  #         echo "${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}" | base64 -d > AuthKey.p8
  #         
  #         # Upload to App Store using xcrun altool
  #         xcrun altool --upload-app \
  #           --type ios \
  #           --file build/ios/ipa/*.ipa \
  #           --apiKey ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }} \
  #           --apiIssuer ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
  #
  #     - name: Cleanup keychain
  #       if: always()
  #       run: |
  #         security delete-keychain build.keychain || true
