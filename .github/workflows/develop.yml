name: Development Deployment

on:
  push:
    branches:
      - develop

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Validate Version Format
        run: |
          chmod +x scripts/validate-version.sh
          ./scripts/validate-version.sh

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: 'stable'

      - name: Get dependencies
        run: flutter pub get

      - name: Create dummy .env file for tests
        run: |
          cat > .env << EOF
          # Test Environment (Dummy values for testing)
          ENVIRONMENT=test
          FIREBASE_DEV_WEB_API_KEY=test
          FIREBASE_DEV_WEB_APP_ID=test
          FIREBASE_DEV_WEB_MEASUREMENT_ID=test
          FIREBASE_DEV_ANDROID_API_KEY=test
          FIREBASE_DEV_ANDROID_APP_ID=test
          FIREBASE_DEV_IOS_API_KEY=test
          FIREBASE_DEV_IOS_APP_ID=test
          FIREBASE_DEV_MESSAGING_SENDER_ID=test
          FIREBASE_DEV_PROJECT_ID=test
          FIREBASE_DEV_AUTH_DOMAIN=test
          FIREBASE_DEV_STORAGE_BUCKET=test
          IOS_BUNDLE_ID=test
          EOF

      - name: Run unit tests
        run: flutter test

  build_web:
    name: Build and Deploy Web (Dev)
    needs: test
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: 'stable'

      - name: Flutter pub get
        run: flutter pub get

      - name: Create .env file from secrets
        run: |
          cat > .env << EOF
          # Development Environment Configuration (Generated by GitHub Actions)
          ENVIRONMENT=dev
          
          # Firebase Development Configuration
          FIREBASE_DEV_WEB_API_KEY=${{ secrets.FIREBASE_DEV_WEB_API_KEY }}
          FIREBASE_DEV_WEB_APP_ID=${{ secrets.FIREBASE_DEV_WEB_APP_ID }}
          FIREBASE_DEV_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_DEV_WEB_MEASUREMENT_ID }}
          FIREBASE_DEV_ANDROID_API_KEY=${{ secrets.FIREBASE_DEV_ANDROID_API_KEY }}
          FIREBASE_DEV_ANDROID_APP_ID=${{ secrets.FIREBASE_DEV_ANDROID_APP_ID }}
          FIREBASE_DEV_IOS_API_KEY=${{ secrets.FIREBASE_DEV_IOS_API_KEY }}
          FIREBASE_DEV_IOS_APP_ID=${{ secrets.FIREBASE_DEV_IOS_APP_ID }}
          FIREBASE_DEV_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_DEV_MESSAGING_SENDER_ID }}
          FIREBASE_DEV_PROJECT_ID=${{ secrets.FIREBASE_DEV_PROJECT_ID }}
          FIREBASE_DEV_AUTH_DOMAIN=${{ secrets.FIREBASE_DEV_AUTH_DOMAIN }}
          FIREBASE_DEV_STORAGE_BUCKET=${{ secrets.FIREBASE_DEV_STORAGE_BUCKET }}
          IOS_BUNDLE_ID=${{ secrets.IOS_BUNDLE_ID }}
          EOF

      - name: Create dummy .env.local
        run: touch .env.local

      - name: Build Web
        run: |
          flutter build web --release \
            --dart-define=ENVIRONMENT=dev \
            --dart-define=FIREBASE_DEV_WEB_API_KEY=${{ secrets.FIREBASE_DEV_WEB_API_KEY }} \
            --dart-define=FIREBASE_DEV_WEB_APP_ID=${{ secrets.FIREBASE_DEV_WEB_APP_ID }} \
            --dart-define=FIREBASE_DEV_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_DEV_MESSAGING_SENDER_ID }} \
            --dart-define=FIREBASE_DEV_PROJECT_ID=${{ secrets.FIREBASE_DEV_PROJECT_ID }} \
            --dart-define=FIREBASE_DEV_AUTH_DOMAIN=${{ secrets.FIREBASE_DEV_AUTH_DOMAIN }} \
            --dart-define=FIREBASE_DEV_STORAGE_BUCKET=${{ secrets.FIREBASE_DEV_STORAGE_BUCKET }} \
            --dart-define=FIREBASE_DEV_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_DEV_WEB_MEASUREMENT_ID }}

      - name: Deploy Firestore Indexes
        run: |
          echo '${{ secrets.MAYPOLE_FIREBASE_SERVICE_ACCOUNT_DEV }}' > $HOME/gcloud-service-key.json
          export GOOGLE_APPLICATION_CREDENTIALS=$HOME/gcloud-service-key.json
          npm install -g firebase-tools
          firebase deploy --only firestore:indexes --project maypole-flutter-dev --non-interactive

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          fireBaseServiceAccount: ${{ secrets.MAYPOLE_FIREBASE_SERVICE_ACCOUNT_DEV }}
          projectId: maypole-flutter-dev
          channelId: live

  # Android deployment complete - commented out to focus on iOS setup
  # build_android:
  #   name: Build and Deploy Android (Dev Internal)
  #   needs: test
  #   runs-on: ubuntu-latest
  #   environment: development
  #   permissions:
  #     contents: write  # Required to push version bump commits
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #       with:
  #         token: ${{ secrets.GITHUB_TOKEN }}
  #         fetch-depth: 0
  #
  #     - name: Auto-Bump Build Number
  #       run: |
  #         chmod +x scripts/auto-bump-build.sh scripts/get-version.sh
  #         git config user.name "github-actions[bot]"
  #         git config user.email "github-actions[bot]@users.noreply.github.com"
  #         git pull --rebase origin develop || echo "⚠️ Pull failed, but continuing..."
  #         echo "📱 Current version:"
  #         ./scripts/get-version.sh
  #         ./scripts/auto-bump-build.sh
  #         echo "✅ New version:"
  #         ./scripts/get-version.sh
  #         if git diff --quiet pubspec.yaml; then
  #           echo "ℹ️ No version changes detected, skipping commit"
  #           exit 0
  #         fi
  #         git add pubspec.yaml
  #         git commit -m "chore: auto-bump build number to $(grep '^version:' pubspec.yaml | sed 's/version: //' | tr -d ' ') [skip ci]"
  #         for i in {1..3}; do
  #           if git push origin develop; then
  #             echo "✅ Successfully pushed version bump"
  #             exit 0
  #           else
  #             echo "⚠️ Push failed (attempt $i/3), pulling and retrying..."
  #             git pull --rebase origin develop
  #           fi
  #         done
  #         echo "❌ Failed to push after 3 attempts"
  #         exit 1
  #
  #     - name: Setup Java
  #       uses: actions/setup-java@v3
  #       with:
  #         distribution: 'temurin'
  #         java-version: '17'
  #
  #     - name: Cache Gradle
  #       uses: actions/cache@v3
  #       with:
  #         path: |
  #           ~/.gradle/caches
  #           ~/.gradle/wrapper
  #         key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
  #         restore-keys: |
  #           ${{ runner.os }}-gradle-
  #
  #     - name: Setup Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.32.0'
  #         channel: 'stable'
  #
  #     - name: Get dependencies
  #       run: flutter pub get
  #
  #     - name: Create .env file from secrets
  #       run: |
  #         cat > .env << EOF
  #         ENVIRONMENT=dev
  #         FIREBASE_DEV_WEB_API_KEY=${{ secrets.FIREBASE_DEV_WEB_API_KEY }}
  #         FIREBASE_DEV_WEB_APP_ID=${{ secrets.FIREBASE_DEV_WEB_APP_ID }}
  #         FIREBASE_DEV_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_DEV_WEB_MEASUREMENT_ID }}
  #         FIREBASE_DEV_ANDROID_API_KEY=${{ secrets.FIREBASE_DEV_ANDROID_API_KEY }}
  #         FIREBASE_DEV_ANDROID_APP_ID=${{ secrets.FIREBASE_DEV_ANDROID_APP_ID }}
  #         FIREBASE_DEV_IOS_API_KEY=${{ secrets.FIREBASE_DEV_IOS_API_KEY }}
  #         FIREBASE_DEV_IOS_APP_ID=${{ secrets.FIREBASE_DEV_IOS_APP_ID }}
  #         FIREBASE_DEV_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_DEV_MESSAGING_SENDER_ID }}
  #         FIREBASE_DEV_PROJECT_ID=${{ secrets.FIREBASE_DEV_PROJECT_ID }}
  #         FIREBASE_DEV_AUTH_DOMAIN=${{ secrets.FIREBASE_DEV_AUTH_DOMAIN }}
  #         FIREBASE_DEV_STORAGE_BUCKET=${{ secrets.FIREBASE_DEV_STORAGE_BUCKET }}
  #         IOS_BUNDLE_ID=${{ secrets.IOS_BUNDLE_ID }}
  #         EOF
  #
  #     - name: Create google-services.json for dev flavor
  #       env:
  #         GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON_DEV }}
  #       run: |
  #         mkdir -p android/app/src/dev
  #         echo "$GOOGLE_SERVICES_JSON" > android/app/src/dev/google-services.json
  #         python3 -m json.tool android/app/src/dev/google-services.json > /dev/null 2>&1 || { echo "❌ Error: Invalid JSON"; exit 1; }
  #         echo "✅ google-services.json created and validated successfully"
  #
  #     - name: Setup Android Signing
  #       run: |
  #         echo '${{ secrets.ANDROID_KEYSTORE_BASE64 }}' | tr -d '\n\r ' | base64 -d > android/app/upload-keystore.jks
  #         cat > android/key.properties << 'KEYSTORE_EOF'
  #         storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}
  #         keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
  #         keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
  #         storeFile=upload-keystore.jks
  #         KEYSTORE_EOF
  #
  #     - name: Build Android App Bundle (devRelease)
  #       run: flutter build appbundle --release --flavor dev
  #
  #     - name: Deploy to Play Store Internal Testing (Draft)
  #       uses: r0adkll/upload-google-play@v1
  #       with:
  #         serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_SERVICE_ACCOUNT_JSON }}
  #         packageName: app.maypole.maypole
  #         releaseFiles: build/app/outputs/bundle/devRelease/app-dev-release.aab
  #         track: internal
  #         status: draft

  build_ios:
    name: Build and Deploy iOS (Dev TestFlight)
    needs: test
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: 'stable'

      - name: Get dependencies
        run: flutter pub get

      - name: Create .env file from secrets
        run: |
          cat > .env << EOF
          ENVIRONMENT=dev
          FIREBASE_DEV_WEB_API_KEY=${{ secrets.FIREBASE_DEV_WEB_API_KEY }}
          FIREBASE_DEV_WEB_APP_ID=${{ secrets.FIREBASE_DEV_WEB_APP_ID }}
          FIREBASE_DEV_WEB_MEASUREMENT_ID=${{ secrets.FIREBASE_DEV_WEB_MEASUREMENT_ID }}
          FIREBASE_DEV_ANDROID_API_KEY=${{ secrets.FIREBASE_DEV_ANDROID_API_KEY }}
          FIREBASE_DEV_ANDROID_APP_ID=${{ secrets.FIREBASE_DEV_ANDROID_APP_ID }}
          FIREBASE_DEV_IOS_API_KEY=${{ secrets.FIREBASE_DEV_IOS_API_KEY }}
          FIREBASE_DEV_IOS_APP_ID=${{ secrets.FIREBASE_DEV_IOS_APP_ID }}
          FIREBASE_DEV_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_DEV_MESSAGING_SENDER_ID }}
          FIREBASE_DEV_PROJECT_ID=${{ secrets.FIREBASE_DEV_PROJECT_ID }}
          FIREBASE_DEV_AUTH_DOMAIN=${{ secrets.FIREBASE_DEV_AUTH_DOMAIN }}
          FIREBASE_DEV_STORAGE_BUCKET=${{ secrets.FIREBASE_DEV_STORAGE_BUCKET }}
          IOS_BUNDLE_ID=${{ secrets.IOS_BUNDLE_ID }}
          EOF

      - name: Create GoogleService-Info.plist for dev
        run: |
          mkdir -p ios/Runner
          echo "${{ secrets.GOOGLE_SERVICE_INFO_PLIST_DEV }}" > ios/Runner/GoogleService-Info.plist
          
          # Validate the plist is well-formed
          if ! plutil -lint ios/Runner/GoogleService-Info.plist > /dev/null 2>&1; then
            echo "Error: GoogleService-Info.plist is not valid"
            cat ios/Runner/GoogleService-Info.plist
            exit 1
          fi
          
          echo "GoogleService-Info.plist created successfully"

      # Configure iOS code signing
      # You will need to:
      # 1. Create certificates and provisioning profiles in Apple Developer Console
      # 2. Export them and add to GitHub Secrets (see DEPLOYMENT_SETUP.md)
      # 3. Add IOS_CERTIFICATE_BASE64 to GitHub Secrets
      # 4. Add IOS_CERTIFICATE_PASSWORD to GitHub Secrets
      # 5. Add IOS_PROVISIONING_PROFILE_BASE64 to GitHub Secrets
      # 6. Add KEYCHAIN_PASSWORD to GitHub Secrets
      - name: Setup iOS Signing
        if: ${{ env.IOS_CERTIFICATE_BASE64 != '' }}
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
        run: |
          # Decode certificate and provisioning profile
          echo "${{ secrets.IOS_CERTIFICATE_BASE64 }}" | base64 -d > certificate.p12
          echo "${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}" | base64 -d > provisioning.mobileprovision
          
          # Create temporary keychain
          security create-keychain -p "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          
          # Import certificate
          security import certificate.p12 -k build.keychain -P "${{ secrets.IOS_CERTIFICATE_PASSWORD }}" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
          
          # Install provisioning profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp provisioning.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: Build iOS IPA (devRelease) - signed
        if: ${{ env.IOS_CERTIFICATE_BASE64 != '' }}
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
        run: |
          flutter build ipa --release --flavor dev --export-options-plist=ios/ExportOptions.plist

      - name: Build iOS (devRelease) - unsigned
        if: ${{ env.IOS_CERTIFICATE_BASE64 == '' }}
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
        run: |
          flutter build ios --release --flavor dev --no-codesign

      # Configure App Store Connect API
      # You will need to:
      # 1. Create an App Store Connect API Key (see DEPLOYMENT_SETUP.md)
      # 2. Add APP_STORE_CONNECT_API_KEY_ID to GitHub Secrets
      # 3. Add APP_STORE_CONNECT_API_ISSUER_ID to GitHub Secrets
      # 4. Add APP_STORE_CONNECT_API_KEY_CONTENT to GitHub Secrets (base64 encoded .p8 file)
      - name: Upload to TestFlight
        if: ${{ env.IOS_CERTIFICATE_BASE64 != '' && env.APP_STORE_CONNECT_API_KEY_CONTENT != '' }}
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
        run: |
          # Decode API key
          echo "${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}" | base64 -d > AuthKey.p8
          
          # Upload to TestFlight using xcrun altool
          xcrun altool --upload-app \
            --type ios \
            --file build/ios/ipa/*.ipa \
            --apiKey ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }} \
            --apiIssuer ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}

      - name: Cleanup keychain
        if: always() && env.IOS_CERTIFICATE_BASE64 != ''
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
        run: |
          security delete-keychain build.keychain || true
